# 进度

## 2026-09-17（Claude）

**G1 完成**
分支：cn
改动：删 `.github/workflows/i18n-guard.yml`、`.github/workflows/crowdin.yml`；README 顶部加二开分支说明
测试：不涉及代码
遗留：无

**K6 完成（海外依赖排查）**
命令：`grep -rnoE "https?://...(googleapis|gstatic|jsdelivr|unpkg|cdnjs|fonts\.|youtube|gravatar|...)" frontend/apps frontend/shared-ui static internal cmd`
结论：**前端与静态模板没有任何海外 CDN / Google Fonts 引用**，字体和 JS 全部打包在本地。仅有三类外部地址：
1. `https://docs.libredesk.io/...` / `https://libredesk.io/` —— 后台各页的「文档」链接和页脚「Powered by」。国内可能打不开但不影响功能；K2 中文化时可把页脚链接改成自己的。
2. `https://updates.libredesk.io/updates.json`（`cmd/updates.go`）—— 启动时检查新版本。部署时 `config.toml` 设 `check_updates = false` 即可，不必改代码。
3. `https://www.googleapis.com/oauth2/...`（`cmd/oauth.go`、`internal/inbox/channel/email/oauth/`）—— 只在配置 Gmail OAuth 收件箱时才会请求，不配置就不会触发。
遗留：无需替换方案。

**i18n 完成**：`i18n/zh-CN.json` 1092 → 1603 键，与 en-US 完全对齐；占位符与复数管道逐键校验通过。

## 2026-09-18（Gemini）

**G2 完成**
分支：cn-feature-switch（合回 cn）
改动：
- `internal/feature/feature.go`（新建：Manager、内存开关映射、Reload、All 等）
- `internal/feature/feature_test.go`（新建：Manager 单元测试）
- `internal/migrations/v2.9.1.go`（新建：初始化 6 个 feature.* 键）
- `cmd/upgrade.go`（注册 v2.9.1 迁移）
- `cmd/middlewares.go`（新增 feat 中间件，关闭返回 404 + featureDisabled 错误）
- `cmd/handlers.go`（按前缀 helper 分组注册 ai/helpcenter/csat/livechat 并包上 feat 中间件）
- `cmd/main.go`（注入 feature.Manager、注册 inbox 邮件轮询开关回调）
- `cmd/init.go`（reloadSettings 中增加 feature.Reload 刷新）
- `cmd/settings.go`（handleGetGeneralSettings 返回 features 字典）
- `internal/inbox/inbox.go`（Manager 增加 SetEmailChannelEnabledFunc，Start 时关闭则跳过轮询）
- `i18n/en-US.json`、`i18n/zh-CN.json`（新增 globals.messages.featureDisabled）
- `cmd/feature_middleware_test.go`（新建：feat 中间件与路由受控测试）
测试：
- `go test -v ./internal/feature/...` → PASS
- `go test -v ./cmd -run "TestFeatMiddleware|TestRegisteredFeatureRoutesGated"` → PASS
- `go vet ./...` → PASS（0 警告）
- `go test ./...` → PASS（全部通过，全绿）
- `cd frontend && pnpm test:run` → PASS（43 test files passed, 799 tests passed）
- `cd frontend && pnpm lint` → 285 errors（均为上游既有历史问题，G2 未动任何前端代码，新增 0 错误）
设计决定：
1. Git 分支层级限制：因已有 `cn` 分支，创建 `cn/<模块>` 会触发 git ref lock 冲突，因此子分支使用 `cn-feature-switch`，完成后已 merge 合入 `cn`。
2. 迁移版本选择：上游已有 `v2.9.0`，为保证 semver 升序自动执行，新迁移文件命名为 `v2.9.1.go`。
3. 路由分组：在 `cmd/handlers.go` 中采用 `registerAIHandlers`、`registerHelpCenterHandlers`、`registerCSATHandlers`、`registerLiveChatHandlers` 进行分组包装，改动处均标有 `// [cn-fork]`。
遗留：无

**G3 完成**
分支：cn-frontend-features（合回 cn）
改动：
- `frontend/apps/main/src/stores/feature.js`（新建：useFeatureStore，管理 6 项功能开关状态及 updateFeature）
- `frontend/apps/main/src/stores/feature.test.js`（新建：useFeatureStore 与 filterNavItems 联动测试，4 个测试用例全部通过）
- `frontend/apps/main/src/views/admin/features/Features.vue`（新建：后台「功能模块」管理页，支持 6 项开关切换与 toast 提示）
- `frontend/apps/main/src/constants/navigation.js`（在工作区组加入「功能模块」入口，helpCenter 与 ai 组关联 feature 开关属性）
- `frontend/apps/main/src/utils/nav-permissions.js`（filterNavItems 扩展支持 isFeatureEnabled 功能开关校验过滤）
- `frontend/apps/main/src/components/sidebar/Sidebar.vue`（侧边栏接入 useFeatureStore，根据开关动态隐藏 helpcenter / ai 菜单）
- `frontend/apps/main/src/router/index.js`（注册 /admin/features 路由；beforeEach 守卫拦截未开启的 AI / HelpCenter 路由并重定向）
- `frontend/apps/main/src/stores/appSettings.js`（fetchSettings 获得 features 数据后自动同步到 useFeatureStore）
- `cmd/settings.go`（handleUpdateGeneralSettings 支持解析 features 字段并批量更新 feature.*.enabled 到 DB）
- `i18n/en-US.json`、`i18n/zh-CN.json`（两边同时添加 admin.features.* 词条）
测试：
- `go test ./...` → PASS（全绿）
- `go vet ./...` → PASS（0 警告）
- `cd frontend && pnpm test:run` → PASS（44 test files passed, 803 passed）
- `cd frontend && pnpm eslint <touched_files>` → PASS（0 errors, 0 warnings）
- 全量 `cd frontend && pnpm lint` → 286 errors（与修改前完全一致，新增代码 0 错误）
遗留：无

## 2026-09-17 协作状态（Claude 记录）

已通过 computer-use 直接把任务派给两边：

- **Gemini（Antigravity，本机仓库）**：G2 已完成并合进 `cn`（提交 `14d9b42a` + merge `3514a69a`），Claude 独立复验 `go build` 通过、`go test ./...` 全绿、`internal/feature` 测试 ok，14 文件 +638/-116。G3 进行中。
  - 偏差记录（已告知，不返工）：分支名用了 `cn-feature-switch` 而非 `cn/<模块>`；额外改了方案未列的 `cmd/init.go`、`cmd/upgrade.go`。
- **Grok 群组（云主机）**：开发bot 完成 K1、K2（分支 `cn/web-templates`，提交 `59e90f88`）；测试bot 认领 K5、K4，K6 已由 Claude 做完并告知其跳过。

⚠️ **待解决的衔接问题**：Grok 侧基线是上游 `f4916f41`，其 `zh-CN.json` 只有约 1113 键，而本机 `cn` 分支已补到 1603 键。已要求开发bot：
1. patch **正文贴到群里**（Claude 无法访问其云主机 `/workspace/`）；
2. patch 中**剔除 i18n 文件**，只保留 `static/` 与 `schema.sql`；
3. 新增的模板引用键**单独贴 JSON**，由 Claude 合进本机语言包；
4. `helpCenter.poweredBy` 本机已存在（「技术支持」），以本机为准。

## 2026-09-17 K1 + K3（Claude 本机落地）

起因：Grok 开发bot 在云主机上做完 K1/K2，但 patch 传不过来——它的 /workspace/ 我访问不到，聊天框贴正文会截断，
而它提出的「GitHub 设备授权」需要用用户账号授权一个外部 bot，**这是只有用户本人能决定的事，已拒绝**。
改为：代码改动由 Claude 在本机落地（本来文件就在本机，且能保证与已补全的语言包一致），
Grok 侧改做本机做不了的部分（开发bot → K5 微信实测，测试bot → K4 docker 集成验收）。

**K1 完成（邮件模板中文化）**
分支：cn
改动：
- `static/email-templates/welcome.html`、`reset-password.html` —— 全部文案改走 `L.T` / `L.Ts` 语言包（模板函数表 `getTmplFuncs` 里的 `L` 在邮件模板同样可用，两者共用 `ParseTemplatesGlob(funcMap, ...)`）
- `static/email-templates/base.html` —— 页脚 "Powered by" 改用已有键 `helpCenter.poweredBy`
- `i18n/en-US.json` / `zh-CN.json` —— 新增 10 个 `email.*` 键
测试：`go build ./...` 通过、`go test ./...` 无 FAIL；en/zh 均 1628 键、零缺失
遗留：无

**K2 复核结论：网页模板无需改动**
扫描 `static/**/*.html` 全部 23 个文件，除上述 3 个邮件模板外，**没有任何裸英文**——
`csat.html`、`error.html`、`info.html`、`index.html`、`help/{classic,docs,shared}/*` 早已全部走 `{{ L.T "key" }}`。
它们显示为英文只是因为 `app.lang` 默认是 en-US，改默认语言即可，不需要动模板。

**K3 完成（默认设置与种子数据中文化）**
改动 `schema.sql`：
- `app.lang` → `zh-CN`，`app.timezone` → `Asia/Shanghai`
- 6 条 AI 编辑器预设提示词：指令与标题全部改中文
- 8 条内置邮件通知模板：正文、`name`、`subject` 全部中文（会话分配 / 联系人回复 / 参与会话回复 / 会话重开 / SLA 预警 / SLA 超时 / 被提及 / 满意度邀请）
- 默认营业时间与默认 SLA 策略的名称和描述改中文
⚠️ **只改 schema.sql，不写迁移**：这些是管理员可在后台编辑的数据，写迁移去覆盖会冲掉已有安装的自定义内容。
因此中文种子**只对全新安装生效**；已有实例需要在后台自行改或手动执行 UPDATE。
测试：单引号配对检查通过（762，偶数）、8 条 templates 完整；`go build` / `go test` 通过

**G4 完成（挂件在 livechat 关闭时静默不渲染）与 G5 完成（邮箱非必填）**
分支：cn-widget-and-auth（合回 cn）
改动：
- `static/widget.js`：fetchWidgetSettings 与 init 拦截 HTTP 404，静默返回 false，不创建 launcher 元素，不创建 iframe，不输出 console.error。
- `frontend/apps/widget/src/main.js`：initWidget catch 块拦截 404 错误，静默退出，不调用 app.mount，不输出 console.error。
- `frontend/apps/widget/src/App.vue`：顶层容器加 v-if="widgetStore.config"，SET_JWT_TOKEN 中 catch 块静默忽略 404 错误。
- `cmd/chat.go`：
  - handleAuthExchange 放宽邮箱必填：claims.Email == "" && claims.PhoneNumber == "" 时才报错；若存在 email 才校验长度。
  - resolveOrCreateExternalContact 增加 app.user 空指针防御保护。
- `i18n/en-US.json`、`i18n/zh-CN.json`：双语同步添加 auth.emailOrPhoneRequired 键，中文措辞为「必须提供电子邮件或手机号。」（遵循纯中文、无英文原词规范）。
- `cmd/auth_exchange_test.go`（新建）：覆盖缺少 external_user_id、同时缺失邮箱与手机号、仅有手机号、仅有邮箱、两者皆有、超长手机号、超长邮箱、以及中英文语言包报错提示等完整场景。
测试：
- `go test ./...` → PASS（全部通过，全绿）
- `go vet ./...` → PASS（0 警告）
- `cd frontend && pnpm test:run` → PASS（44 test files passed, 803 passed）
- `cd frontend && pnpm eslint apps/widget/src/App.vue apps/widget/src/main.js` → PASS（0 errors, 0 warnings）
设计决定：
1. 关于分支名斜杠：因本地已有 `cn` 主分支，在 Git 底层机制中 `refs/heads/cn` 为文件，导致无法创建 `cn/<模块>` 目录层级分支，故任务分支使用 `cn-widget-and-auth`，验证完合并回 `cn`。
2. 静默防护：做到了三层防护（widget.js 拦截 404 不生成 DOM、main.js 拦截 404 不挂载 Vue、App.vue 拦截 404 与 JWT exchange 错误），实现真正无按钮、无控制台报错。
3. 身份去重确认：`resolveOrCreateExternalContact` 已完全基于 `external_user_id` 唯一约束去重与 upsert（`ON CONFLICT (external_user_id)`），下游所有涉及 email 处均有空值保护，无邮箱用户可稳定持久化与流转。
**G6 完成（已读回执）与 G7 完成（客服接待上限 + 联系人拉黑）**
分支：cn-g6-g7（合回 cn）
改动：
- `internal/migrations/v2.9.2.go`（新建）：紧随 v2.9.1 排布，为 `conversation_messages` 增加 `seen_at TIMESTAMPTZ NULL` 及索引；为 `users` 增加 `max_open_conversations INT DEFAULT 0 NOT NULL`。
- `cmd/upgrade.go`：向 `migList` 注册 `{"v2.9.2", migrations.V2_9_2}`。
- `schema.sql`：同步在 `conversation_messages` 增加 `seen_at` 字段及索引；在 `users` 增加 `max_open_conversations` 字段。
- `internal/conversation/models/models.go`：`ChatMessage` 和 `Message` 增加 `SeenAt null.Time` 字段。
- `internal/conversation/queries.sql`：`get-message` 与 `get-messages` 增加 `seen_at` 查询字段；新增 `mark-incoming-messages-seen` 与 `mark-outgoing-messages-seen` 批量标记语句。
- `internal/conversation/conversation.go`：
  - 预编译上述标记 SQL。
  - `UpdateUserLastSeen`（客服打开会话）执行 `MarkIncomingMessagesSeen`，并通过 WS 广播 `agent_last_seen_at` 到挂件。
  - `UpdateConversationContactLastSeen`（访客/联系人查看会话）执行 `MarkOutgoingMessagesSeen`，向客服后台推已读更新。
  - `BuildWidgetConversationResponse` 复制 `SeenAt`。
- `frontend/apps/main/src/features/conversation/message/MessageBubble.vue`：客服端消息气泡支持根据 `message.seen_at` 显示已读双勾。
- `frontend/apps/widget/src/components/ChatMessages.vue`：挂件消息支持根据 `message.seen_at` 或 `agent_last_seen_at` 显示已读标记。
- `internal/team/models/models.go` & `internal/team/queries.sql`：`TeamMember` 增加 `MaxOpenConversations` 字段并在 `get-team-members` 中联合查询。
- `internal/user/models/models.go` & `internal/user/queries.sql`：`User` 增加 `MaxOpenConversations` 并在 `get-user` 及 `update-agent` 中支持读写。
- `internal/user/agent.go` & `cmd/users.go`：`UpdateAgent` 与 `handleCreateAgent` / `handleUpdateAgent` 接入 `max_open_conversations`。
- `internal/autoassigner/autoassigner.go`：在轮询池中记录每位客服的 `MaxOpenConversations`，并在分配循环中跳过已达接待上限的客服。
- `internal/autoassigner/autoassigner_test.go`（新建）：添加客服接待上限跳过与全员满员拒绝领取的完整单元测试。
- `cmd/widget_middleware.go` & `cmd/chat.go`：联系人拉黑与禁用机制（`!u.Enabled`）拦截，挂件发消息、开局初始化、JWT 身份交换均直接返回 HTTP 403 Forbidden 并提示 `widget.accessDenied`。
- `frontend/apps/main/src/features/admin/agents/formSchema.js` & `AgentForm.vue`：客服表单增加「同时接待上限」字段输入与验证（0 为无限制）。
- `i18n/en-US.json` & `i18n/zh-CN.json`：双语同步添加 `agent.maxOpenConversations`、`agent.maxOpenConversationsHelp` 及 `widget.accessDenied`，中文纯中文无中英并列。
测试：
- `go test ./...` → PASS（全部通过，全绿）
- `go vet ./...` → PASS（0 警告）
- `cd frontend && pnpm test:run` → PASS（44 test files passed, 803 passed）
- `cd frontend && pnpm eslint <touched_files>` → PASS（0 errors, 0 warnings）
- 语言包键值对齐：en 1632 键，zh 1632 键，零缺失。
遗留：无
